/** Fixed header height (px). Used as the scroll offset for anchor navigation. */
export const HEADER_HEIGHT = 76;

/** Standard anchor-scroll offset so targets clear the sticky header. */
export const SCROLL_OFFSET = -(HEADER_HEIGHT - 8);
